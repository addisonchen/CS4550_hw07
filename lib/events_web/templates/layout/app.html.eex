<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Events</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <script type="text/javascript" src="https://cdn.jsdelivr.net/jquery/latest/jquery.min.js"></script>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/momentjs/latest/moment.min.js"></script>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.css" />
    
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <div role="main" class="container">
      <div class="row" style="height: 25px;"></div>
      <div class="row">
        <div class="col-sm-4 flex-row left">
          <span><%= link "Home", to: Routes.page_path(@conn, :index), class: "btn btn-success" %></span>
          <div style="width: 20px;"></div>
          <span><%= link "Users", to: Routes.user_path(@conn, :index), class: "btn btn-primary" %></span>
          <div style="width: 20px;"></div>
          <span><%= link "Meetings", to: Routes.meeting_path(@conn, :index), class: "btn btn-primary" %></span>
        </div>
        <%= if @conn.assigns[:current_user] do %>
          <div class="col-sm-8 flex-row right">
            Hello, <%= @current_user.name %>!
            <div style="width: 20px"></div>
            <%= link "Profile", to: Routes.user_path(@conn, :show, @current_user), class: "btn btn-primary" %>
            <div style="width: 20px"></div>
            <%= link "+ Meeting", to: Routes.meeting_path(@conn, :new), class: "btn btn-primary" %>
            <div style="width: 20px"></div>
            <%= link("Logout", to: Routes.session_path(@conn, :delete), method: :delete, class: "btn btn-danger") %>
          </div>
        <% else %>
          <div class="col-sm-8">
            <div class="flex-row right">
              <%= form_for @conn, Routes.session_path(@conn, :create),
              [class: "form-inline"], fn f -> %>
                <%= text_input f, :email, class: "form-control" %>
                <%= submit "Login", class: "btn btn-primary" %>
              <% end %>
              <div style="width: 30px"></div>
              <button type="button" class="btn btn-secondary"><%= link "Create an account", to: Routes.user_path(@conn, :new), class: "whiteLink" %></button>
            </div>
          </div>
        <% end %>
      </div>



      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info margin" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger maring" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>

      <div style="height: 50px"></div>

      <%= @inner_content %>
    </div>
  </body>
</html>
