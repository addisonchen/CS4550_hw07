<h1>New Meeting</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.meeting_path(@conn, :create)) %>
<div style="height: 20px"></div>
<span><%= link "Cancel", to: Routes.page_path(@conn, :index), class: "btn btn-danger" %></span>
